/**
 * Example Route File
 * 
 * Responsibility: Defines API endpoints and maps them to controller functions.
 * Routes handle HTTP methods (GET, POST, PUT, DELETE) and request/response flow.
 * 
 * Structure:
 * - Define route paths
 * - Apply middleware (authentication, validation, rate limiting)
 * - Connect routes to controller functions
 * - Group related endpoints
 * 
 * Example usage:
 * const express = require('express');
 * const router = express.Router();
 * const { getExample, createExample } = require('../controllers/exampleController');
 * 
 * router.get('/', getExample);
 * router.post('/', createExample);
 * 
 * module.exports = router;
 */

// This is an example file showing the structure
// Replace with your actual route files
