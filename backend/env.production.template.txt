# ============================================
# OCKRIX Backend Production Environment
# ============================================
# Copy these values to your deployment platform (Render/Railway)
# Never commit actual values to version control
# ============================================

# Server Configuration
NODE_ENV=production
PORT=3000
# Note: Render uses PORT=10000, Railway uses PORT=3000

# JWT Authentication
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secure-jwt-secret-minimum-64-characters-change-this-immediately

# Token Configuration
TOKEN_EXPIRY_MINUTES=60

# Rate Limiting
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX=100

# CORS Configuration
# Comma-separated list of allowed origins
# Example: https://your-frontend.vercel.app,https://your-custom-domain.com
ALLOWED_ORIGINS=https://your-frontend.vercel.app

# Database (when implemented)
# DATABASE_URL=your-database-connection-string

# Redis (optional, for distributed rate limiting)
# REDIS_URL=redis://your-redis-url:6379

# Logging
LOG_LEVEL=info
