/**
 * Example Utility File
 * 
 * Responsibility: Contains pure, reusable helper functions with no side effects.
 * Utilities are stateless and don't depend on request context.
 * 
 * Common utilities:
 * - Data formatting (dates, strings, numbers)
 * - Validation helpers
 * - Hash/encrypt functions
 * - File operations
 * - Email sending
 * - Token generation
 * - Pagination helpers
 * 
 * Example structure:
 * 
 * exports.formatDate = (date) => {
 *   return new Date(date).toISOString();
 * };
 * 
 * exports.validateEmail = (email) => {
 *   const regex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
 *   return regex.test(email);
 * };
 * 
 * exports.generateRandomString = (length) => {
 *   // Implementation
 * };
 */

// This is an example file showing the structure
// Replace with your actual utility files
